# Runtime dependencies, as actually imported by src/, scripts/ and tests/.
#
# Lower bounds are what the code needs; the exact versions the reported
# results were produced with are pinned in ENVIRONMENT.md, which also records
# the GPU, driver and CUDA build. Install those if you need to reproduce the
# JSON artefacts in results/honest/ bit-for-bit.
#
# Note the numpy bound: results were produced under numpy 2.x. An earlier
# version of this file pinned <2.0.0, which did not match the environment the
# experiments ran in.

torch>=2.0.0
torchvision>=0.15.0

numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.2.0

matplotlib>=3.7.0
Pillow>=9.5.0

# Backbone used to build weights/caches/*.pkl. Not needed to re-run the
# experiments themselves, which read the cached embeddings.
facenet-pytorch>=2.3.0

pytest>=7.0.0

# Not required. qiskit and qiskit-aer were listed previously but are not
# imported anywhere in the active tree: the SWAP-test simulator is implemented
# directly in PyTorch (src/quantum_core.py, src/quantum_fast.py) so that
# gradients flow. torchaudio, jupyter and ipython were likewise unused.
